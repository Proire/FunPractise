// types/User.ts
export interface User {
    id: number;
    name: string;
    phone: string;
    email: string;
    website: string;
    dob: string; // Assuming dob is stored as a string in ISO format ("YYYY-MM-DD")
    address: string;
  }
  